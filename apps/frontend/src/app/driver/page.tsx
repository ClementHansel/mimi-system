import { DriverJobsPanel } from '@/components/driver/DriverJobsPanel';

/**
 * F13 `driver` — mobile, offline-first (BUILD-PLAN W4-09). Today's Surat
 * Jalan, multi-drop route in sequence: depart → arrive → serah terima per
 * drop, cold-chain seal/temperature checks throughout.
 */
export default function DriverPage() {
  return <DriverJobsPanel />;
}
